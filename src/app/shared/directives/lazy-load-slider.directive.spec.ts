import { LazyLoadSliderDirective } from './lazy-load-slider.directive';

describe('LazyLoadSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyLoadSliderDirective();
    expect(directive).toBeTruthy();
  });
});
